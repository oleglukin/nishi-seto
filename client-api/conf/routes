# Routes
# This file defines all application routes (Higher priority routes first)
# https://www.playframework.com/documentation/latest/ScalaRouting
# ~~~~

# An example controller showing a sample home page
GET     /                           controllers.HomeController.index

# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file               controllers.Assets.versioned(path="/public", file: Asset)

# Location routes
GET     /api/location/list                  controllers.LocationController.getListOfKnownLocations
GET     /api/location/agg-:locationname     controllers.LocationController.getAggregationByLocation(locationname)
GET     /api/location/all                   controllers.LocationController.getAllAggregations
GET     /api/location/totalevents           controllers.LocationController.getTotalEvents
POST    /api/location/agg-:locationname     controllers.LocationController.newLocationAggregation(locationname)
DELETE  /api/location/agg-:locationname     controllers.LocationController.removeAggregation(locationname)
DELETE  /api/location/all                   controllers.LocationController.clearAllAggregations